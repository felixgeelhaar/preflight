---
title: Preflight
description: Set up a new Mac in 10 minutes from one YAML file. Capture your machine, push to git, bootstrap any new laptop deterministically.
template: splash
hero:
  tagline: Set up a new Mac in 10 minutes — from one YAML file you keep in git.
  actions:
    - text: Get Started
      link: /preflight/getting-started/introduction/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/felixgeelhaar/preflight
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## The killer loop

```bash
preflight capture     # snapshot current machine to YAML
git push              # versioned in your dotfiles repo
preflight apply       # bootstrap any new laptop deterministically
```

Preflight is the missing piece between your dotfiles repo and a usable laptop — packages, dotfiles, shell, editor, all in one config.

## Why Preflight?

<CardGrid stagger>
  <Card title="Capture → Bootstrap" icon="rocket">
    Snapshot your current setup, commit to git, restore on any new machine. End-to-end loop no other tool ships out of the box.
  </Card>
  <Card title="Plan Before Apply" icon="approve-check">
    Always preview changes before executing. Every action is explained with context and documentation.
  </Card>
  <Card title="Deterministic Execution" icon="document">
    Same config always produces the same result. Re-running apply is always safe.
  </Card>
  <Card title="Multi-Platform" icon="laptop">
    macOS (Homebrew), Linux (apt), Windows (winget/scoop/choco). Editor support for Neovim and VSCode.
  </Card>
</CardGrid>

## Enterprise Features

<CardGrid stagger>
  <Card title="Enterprise Identity" icon="shield">
    OIDC device authorization flow for enterprise trust chains, plugin signatures, and fleet access control.
  </Card>
  <Card title="SLSA Attestation" icon="approve-check-circle">
    Verify reproducible builds with SLSA provenance, in-toto statements, and Sigstore keyless signing.
  </Card>
  <Card title="Cloud Fleet Discovery" icon="cloud">
    Auto-discover hosts from AWS EC2, Azure VMs, and GCP Compute Engine for fleet management.
  </Card>
  <Card title="Compliance Attestation" icon="document">
    Cryptographic proof that machines met policy requirements at a point in time.
  </Card>
</CardGrid>

## The Compiler Model

```
Intent → Merge → Plan → Apply → Verify
```

Preflight treats workstation setup as a **compilation problem**, not a collection of scripts:

1. **Load** - Parse manifest and layer overlays
2. **Merge** - Deep merge with provenance tracking
3. **Plan** - Diff current state vs desired
4. **Apply** - Execute idempotently
5. **Verify** - Doctor checks for drift

## Quick Example

```yaml
# preflight.yaml
name: my-workstation
target: macos

layers:
  - base
  - identity.work
  - role.go

packages:
  brew:
    formulae:
      - git
      - gh
      - ripgrep

shell:
  default: zsh
  starship:
    enabled: true
```

```bash
# Preview changes
preflight plan

# Apply configuration
preflight apply
```

## Core Guarantees

| Guarantee | Description |
|-----------|-------------|
| No execution without a plan | Always show what will change first |
| Idempotent operations | Re-running apply is always safe |
| Explainability | Every action has "why" and documentation |
| Secrets never leave | Config uses references, not values |
| User ownership | Portable, inspectable, git-native |
