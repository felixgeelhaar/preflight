version: 2

project:
  name: preflight
  repo: felixgeelhaar/preflight

versioning:
  strategy: conventional
  initial: 0.1.0

changelog:
  enabled: true
  path: CHANGELOG.md

release:
  github:
    enabled: true
    draft: false
    prerelease: false

# Note: Build artifacts and release uploads are handled by GoReleaser
# See .goreleaser.yaml for build and release configuration
